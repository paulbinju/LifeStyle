    <div class="panel panel-default" style="border-color:#3453a4;">
        <div class="panel-heading" style="font-size:16px; font-weight:bold;background-color:#3453a4;color:#fff;">
            My Followers
        </div>
        <div class="panel-body">
            @{

        foreach (var flws in ViewBag.Followers)
        {
            <div class="col-md-2 text-center" style="margin-right:5px;padding:10px; background-color:#fafafa;">

                <div class="card">
                    @if (flws.ProfileImage != null && flws.ProfileImage != "")
            {
                <a href="@Url.Action("Author", "Home" , new { UserId=flws.UserId })">
                    <img src="@Url.Content(flws.ProfileImage)" class="img-thumbnail img-responsive" style="width:100%; height:150px" />
                </a>
    }
    else
    {
        <a href="@Url.Action("Author", "Home" , new { UserId=flws.UserId })">
            <img src="@Url.Content("~/images/user.png")" class="img-thumbnail img-responsive" style="width:100%; height:150px" />
        </a>
}
                    <p><button>@flws.FullName</button></p>
                </div>


                @*<div class="btn btn-danger" id="btn_@flws.Id" onclick="follow(@flws.UserId,@flws.Id);">
                Followback
                <i class="fa fa-plus-square"></i>
            </div>*@
                @{
            string followback = "No";
                }

                @foreach (var flusers in ViewBag.followedUsers)
        {

            if (flws.UserId == flusers.FollwerId)
            {
                followback = "Yes";
                @**@
    }
}

                @if (followback == "No")
        {
            <div class="btn" style="color:#fff;background-color:#3453a4;" id="btnFollow_@flws.Id" onclick="follow(@flws.UserId,@flws.Id);">
                Followback
                <i class="fas fa-user-tag"></i>
            </div>
}


        @*@if (Convert.ToInt32(flws.UserId) != Convert.ToInt32(ViewBag.LoggedInUserId))
    {
    <div class="btn btn-danger" id="btn_@flws.Id" onclick="follow(@flws.UserId,@flws.Id);">
        Followback
        <i class="fa fa-plus-square"></i>
    </div>
    }*@

            </div>
}

            }
        </div>
        <div class="panel-footer" style="background-color:#3453a4;"></div>
    </div>
    
    


